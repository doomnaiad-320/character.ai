# 前端 UI 重构与迁移方案草案

> 目标：在新项目中重写前端 UI，并逐步从旧界面迁移，降低维护成本、统一设计与交互，同时控制上线风险。

---

## 1. 重构目标与边界

### 1.1 重构目标

- **统一视觉和交互**：
  - 建立统一的设计系统（颜色、字号、间距、组件规范）。
  - 表单、表格、弹窗等核心交互有一致的行为和风格。
- **提高可维护性**：
  - UI 与业务逻辑解耦，减少页面中的散落逻辑和重复代码。
  - 按业务模块（Domain）组织代码，清晰的模块边界。
- **提升可扩展性**：
  - 为后续新功能、新产品线留出扩展位，避免再次大规模返工。

### 1.2 不在本次重构范围内的内容

- 不大规模重写后端服务逻辑，仅做必要的接口封装与字段兼容处理。
- 不改变业务规则和主流程（例如审批规则、计算规则），只调整前端呈现与交互。
- 不一次性迁移所有边角功能，低频模块可延后或保留在旧 UI 中一段时间。

### 1.3 迁移原则

- **新旧共存**：重构期间，旧前端保持可用，支持随时回退。
- **渐进迁移**：按业务模块/功能块逐步迁移，而非一次性切换。
- **可回滚**：每个迁移里程碑都要有明确的回滚方案（如何快速切回旧 UI）。

---

## 2. 新前端架构设计（技术栈无关）

> 以下为逻辑分层与目录结构设计，可套用在 React/Vue 等栈上。

### 2.1 分层结构

#### 2.1.1 app 层

- 职责：
  - 应用入口（渲染根组件）。
  - 路由定义与守卫（权限、登录态检查）。
  - 全局布局（顶栏、侧边栏、面包屑）。
  - 国际化和全局状态注入（如全局 Store Provider）。
- 示例目录：
  - `src/app/index`（入口）
  - `src/app/router`（路由配置）
  - `src/app/layouts`（基础布局）

#### 2.1.2 features / modules 层（按业务域）

- 每个业务域一个模块，例如：
  - `src/features/auth`（登录、注册、权限）
  - `src/features/projects`（项目列表、详情、创建/编辑）
  - `src/features/orders`（订单相关流程）
  - `src/features/settings`（系统/个人设置）
- 每个 feature 内建议结构：
  - `pages/`：该模块的页面组件（与路由对应）。
  - `components/`：该模块专用的 UI 组件（不跨模块复用）。
  - `api/`：该模块的 API 封装，如 `getProjectList`、`updateProject`。
  - `hooks/` 或 `services/`：该模块的业务逻辑聚合，如 `useProjectList`、`useProjectDetail`。
  - `types/`：与后端约定的数据结构定义（DTO、接口类型等）。

#### 2.1.3 shared/ui 层（通用组件库）

- 存放可跨业务模块复用的 UI 组件：
  - 基础组件：`Button`、`Input`、`Select`、`Checkbox`、`Radio` 等。
  - 布局组件：`PageHeader`、`Card`、`Grid`、`Panel` 等。
  - 复合组件：`Form`、`Table`、`Modal`、`Drawer`、`Tabs` 等。
- 原则：
  - 尽量不包含业务逻辑，只处理视觉与交互模式。
  - 对第三方组件库进行二次封装，形成统一的业务内 API（避免在业务代码中直接到处引用第三方组件库细节）。

示例目录：

- `src/shared/ui/button`
- `src/shared/ui/input`
- `src/shared/ui/table`
- `src/shared/ui/modal`

#### 2.1.4 shared/lib & shared/services 层（通用逻辑）

- `src/shared/lib/`
  - `httpClient`：统一的 HTTP 客户端封装（基于 fetch/axios 等）。
  - `validators`：表单验证函数。
  - `date` / `number`：日期和数字相关工具方法。
- `src/shared/services/`
  - 与多模块相关的跨域逻辑（如全局搜索、通知、消息等）。
- `src/shared/config/`
  - 环境变量、常量、枚举等配置。

### 2.2 设计系统与组件库策略

#### 2.2.1 Design Tokens

- 统一定义以下变量：
  - 颜色：主色、辅色、背景色、文本色、边框色等。
  - 字体：字号、行高、字体族。
  - 间距：`spacing-xs`, `spacing-sm`, `spacing-md`, `spacing-lg` 等。
  - 圆角、阴影：卡片、弹窗、按钮的圆角和阴影标准。
- 表示方式：
  - 使用一个集中声明文件，如 `src/shared/config/theme.(ts|json)`。

#### 2.2.2 交互规范

- 表单：
  - 提交前的校验与错误提示样式统一。
  - 提交中 Loading 状态统一。
- 表格：
  - 分页、排序、筛选控件的位置与交互统一。
  - 空状态、错误状态统一展示方式。
- 弹窗：
  - 标题、内容区、底部按钮排列方式一致。
  - 关闭行为（点遮罩/ESC 是否关闭）统一。

### 2.3 工程化约定

- 使用 TypeScript 保证类型安全。
- 使用 ESLint + Prettier 保证代码风格统一。
- 使用 Jest/Vitest + Testing Library 做基础单测（组件和关键业务逻辑）。
- 通过 `.env` 管理不同环境接口地址和配置。

---

## 3. 新项目前期搭建步骤

> 目标：先搭好骨架和基础设施，再迁移业务模块。

### 3.1 初始化项目

1. 创建新前端项目（如：基于 React/Vue 的脚手架）。
2. 建立基础目录结构：

```text
src/
  app/
    index.(tsx|ts|js)
    router/
    layouts/
  features/
    auth/
    projects/
  shared/
    ui/
    lib/
    services/
    config/
```

3. 配置：
   - ESLint、Prettier、git hooks（可选）。
   - 测试框架基础配置。

### 3.2 基础设施优先

1. **HTTP 客户端**
   - 位置：`src/shared/lib/httpClient.(ts|js)`。
   - 能力：
     - 统一设置 baseURL、超时、公共 Header（如 Token）。
     - 拦截器：处理登录态失效、通用错误提示。

2. **全局布局 & 路由**
   - 在 `src/app/router` 中定义路由表，按业务模块拆分。
   - 在 `src/app/layouts` 中实现主布局（顶部导航、侧边栏、内容区）。
   - 实现登录页和一个简单的示例页面，保证项目可跑通。

3. **基础组件**
   - 在 `src/shared/ui` 中实现：
     - `Button`
     - `Input`
     - `Form`（或基于组件库二次封装）
     - `Table`
   - 这些组件先用在登录页、示例页面中，验证设计系统效果。

---

## 4. 迁移策略与路径设计

### 4.1 新旧系统共存方式（备选）

#### 方案 A：按路径在网关/反向代理层分流

- 路径规划示例：
  - 新 UI：`/app/*`
  - 旧 UI：`/legacy/*` 或其他原有路径。
- 操作方式：
  - 在 Nginx 或 API 网关配置中，根据 URL 前缀将请求转发到不同前端应用。
- 优点：
  - 技术简单，耦合度低。
  - 便于灰度：部分业务切到 `/app`，未完成的继续用旧地址。

#### 方案 B：主入口为新 UI，部分功能跳转到旧 UI

- 用户一进入新 UI（如 `/app`），所有导航、布局都在新前端中。
- 还未迁移的模块，在菜单点击时直接 `window.location.href` 跳转到旧系统对应 URL。
- 适合：
  - 过渡期较长，但希望用户「感觉」主要界面已经换新。

#### 方案 C：微前端 / iframe（仅在必要时）

- 将部分不打算短期重写的旧模块作为子应用嵌入（微前端）或 iframe。
- 适用于：
  - 技术栈差异巨大且难整合。
  - 有些模块极其复杂、风险极高，短期内不重写。

> 初期推荐：**优先考虑方案 A + 部分菜单跳转逻辑**，实现简单且风险可控。

### 4.2 模块迁移顺序

迁移顺序建议：

1. **登录 / 权限 / 布局框架（MVP 步骤）**
   - 新 UI 提供登录页面和主框架，让用户「看得见变化」。
   - 即使业务模块还在旧 UI，登录后从新壳中跳转过去。

2. **首个核心业务模块（模块 A）**
   - 选择一个最核心、相对独立的业务流程，如：
     - 项目列表 + 项目详情 + 新建/编辑项目。
   - 在新 UI 中完整重写：页面、交互、调用同一后端接口。

3. **围绕模块 A 的周边功能**
   - 搜索、筛选、导出、批量操作等。
   - 把与模块 A 相关的所有页面都转到新 UI，做到「该域完整迁移」。

4. **其他重要模块（模块 B、C…）**
   - 按优先级和复杂度依次迁移。
   - 每迁完一个模块，记录：
     - 替代的旧 URL 列表。
     - 新旧功能的差异说明。
     - 回滚方式。

5. **长尾/低频模块 & 报表类**
   - 优先级最低，可以：
     - 简化为只读版。
     - 延迟重写，甚至长时间保留在旧 UI。

---

## 5. API 与业务逻辑治理

### 5.1 统一 API 层

- 为每个业务模块建立自己的 API 文件：
  - `src/features/projects/api.ts`
  - `src/features/orders/api.ts`
- API 文件内部：
  - 只调用 `shared/lib/httpClient`。
  - 对外暴露语义化方法，如 `fetchProjectList`, `createProject`, `updateProject`。
- 页面和 hooks：
  - 不直接写 `fetch/axios`，只调用这些 API 函数。

### 5.2 业务逻辑通过 hooks / services 聚合

- 每个业务模块内：
  - `hooks/useProjectList.ts`：处理列表数据加载、分页、筛选、loading 状态。
  - `hooks/useProjectDetail.ts`：处理详情数据获取、更新操作、错误处理。
- 页面组件职责：
  - 只负责：
    - 组合 UI 组件。
    - 绑定事件（点击按钮 → 调用 hook 提供的 action）。
  - 不进行复杂数据处理或流程控制。

### 5.3 与后端的契约管理

- 使用类型定义/接口定义（如 TypeScript 的 interface/type）约束接口输入输出。
- 对每个接口的变更：
  - 需要有变更记录（Changelog）。
  - 优先保证向后兼容，必要时在前端做兼容层。

---

## 6. 里程碑规划（示例）

> 可根据团队实际节奏调整，这里作为参考。

### 里程碑 M1：新前端骨架 & 壳完成（约 2–3 周）

- 完成内容：
  - 新前端项目初始化、工程化工具配置。
  - 设计系统基础：颜色、排版、基础组件（Button/Input）。
  - HTTP 客户端完成，能访问实际后端接口。
  - 路由系统 & 全局布局（顶部/侧边栏）搭建完毕。
  - 登录页在新 UI 中实现并可正常登录（即使后续仍跳转到旧 UI）。

### 里程碑 M2：首个核心业务模块迁移（约 2–4 周）

- 选择「模块 A」（例如项目管理）：
  - 项目列表页（查询、分页、筛选）。
  - 项目详情页。
  - 新建/编辑项目弹窗或页面。
- 要求：
  - 与旧 UI 功能等价或更优。
  - 有监控和错误日志，能快速定位问题。
  - 有基础测试覆盖（关键 hooks 和组件）。

### 里程碑 M3：扩展到多个模块 & 完善体验（约 4–6 周）

- 迁移模块 B、模块 C……
- 补齐：
  - 统一错误处理。
  - 埋点和统计。
  - 性能优化（首屏加载、懒加载等）。

### 里程碑 M4：长尾模块 & 老 UI 下线（时间视复杂度而定）

- 处理长尾功能：
  - 迁移或降级为只读。
- 对外宣布：
  - 老 UI 的下线时间表。
  - 新 UI 的使用说明和差异点。
- 技术收尾：
  - 网关配置调整，默认全部走新 UI。
  - 清理新前端中残留的旧系统依赖和跳转逻辑。

---

## 7. 立即可执行的行动清单

1. **撰写正式的《前端重构目标与范围》文档**（可基于本文件第 1 章扩展）。
2. **梳理当前系统页面/模块地图**：
   - 按业务域列出所有页面和功能。
   - 标注：优先级（高/中/低）、复杂度、是否必须迁移。
3. **选定技术栈与组件库**：
   - 如 React + TS + 某组件库，或 Vue3 + TS + 某组件库。
4. **确定新旧前端路由/部署策略**：
   - 例如采用「`/app/*` 新 UI，其他走旧 UI」的方案。
5. **搭建新前端项目骨架**：
   - 完成第 3 章中列出的初始化和基础设施。
6. **确定「模块 A」（首个迁移模块）并细化需求**：
   - 列出模块 A 在旧系统中的所有页面和功能点，作为新 UI 的对照清单。

---

> 后续可以根据实际技术栈（React/Vue 等）和业务模块列表，对本草案做一次「栈相关」细化：包括具体目录命名、状态管理方案选择、首批组件和 hooks 的详细设计等。